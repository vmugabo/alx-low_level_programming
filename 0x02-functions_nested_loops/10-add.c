#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: add to b
 * @b: add to a
 * Return:V  sum of a and b
 */
int add(int a, int b)
{
	int V;

	V = a + b;

	return (V);
}
